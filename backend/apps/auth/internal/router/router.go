package router

import (
	"github.com/morehao/golib/biz/gserver/ginserver"
)

func RegisterRouter(groups *ginserver.RouterGroups, appName string) {
	// Task 4 填入 authRouter / personRouter / userSessionRouter
}
